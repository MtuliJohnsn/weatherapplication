<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Lab 02</title>
    <link rel="stylesheet" href="/css/style.css">
  </head>
  <body>
    <form action="/" method="post">
      <h1>Weather Information</h1>
      <input name="city" type="text" id="city" placeholder="Enter a City" required>
      <button type="submit">Get Weather</button>
    </form>

    <% if (weather) { %>
      <div class="card">
        <p><%= weather.city %> <%= weather.currentDate %></p>
        <p1><%= weather.description %></p1>
        
        <div class="icon">
          <img src="http://openweathermap.org/img/w/<%= weather.icon %>.png">
        </div>
        <div class="content">
          <p>Temperature:</p> 
          <p><%= weather.temperature %> &deg;C</p>

          <p>Feels like:</p> 
          <p><%= weather.feels_like %> &deg;C</p>

          <p>Humidity:</p> 
          <p><%= weather.humidity %>%</p>

          <p>Wind speed:</p> 
          <p><%= weather.wind %> mph</p>
        </div>
      </div>
    <% } %>
    <% if (error) { %>
      <p><%= error %></p>
    <% } %>
  </body>
</html>
